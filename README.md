# 待办表 Windows C++ 程序

## 功能
- 新增待办事项
- 支持日期
- 支持可选时间
- 勾选待办项后，自动移动到“已完成”列表

## 构建方式
这个工程使用 CMake + Win32 API。

1. 用 Visual Studio 2022 或更高版本打开整个文件夹
2. 让 CMake 生成工程
3. 编译并运行 `TodoListWin32`

## 说明
- 该程序当前不做数据持久化，重启后列表会清空
- 日期使用系统日期选择控件，时间可通过“包含时间”勾选后启用
